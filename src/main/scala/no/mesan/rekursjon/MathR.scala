package no.mesan.rekursjon

object MathR {

  // Implementer denne slik at den finner det Nte tallet i Fibonacci-rekken
  // (definert ved fib(0)=0, fib(1)=fib(2)=1, fib(n)=fib(n-1)+fib(n-2)
  def fib(n:Int): BigInt = {
    BigInt(n) // Nei...
  }

  // Implementer denne slik at den finner N! (=1*2*....*N), fac(0)=0
  def fac(n:Int):BigInt = {
    BigInt(n) // Ei heller
  }

}